services:
  - type: web
    name: dingtalk-reminder
    env: python
    pythonVersion: "3.11"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dingtalk_reminder:app --bind 0.0.0.0:$PORT
